# Checkpoints and config files
CHECKPOINTS_PATH_HOST=/home/data/TalkToDocs-Ingest/debug/cube_checkpoints

CHECKPOINT_PATH_IMG=/checkpoints
CONFIG_PATH_IMG=${CHECKPOINT_PATH_IMG}/pipeline_configs_40.yaml


# Ports

LAYOUT_MODEL_PORT=21200
LINE_MODEL_PORT=21201
TEXTREC_PORT=21202
PIPELINE_PORT=21203
VISION_MODEL_PORT=21205
CHART_DETECTION_PORT=21206

UI_PORT=21204

# OTEL collector on external network: dps-test-network-cube_test
# Port 4317 = gRPC
OTEL_LOGS_ENDPOINT="cube_test-lgtm-1:4317"

LOG_DEP_ENV=test

# Logging level: DEBUG, INFO, WARNING, ERROR (default: INFO)
# Set to DEBUG for detailed pipeline tracing
LOG_LEVEL=INFO

JWT_TOK="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJkcHMtc2VydmljZSIsImF1ZCI6ImRzLWFwaSIsImlhdCI6MTc3MDgwMzMyNywiZXhwIjoxODA2ODAzMzI3fQ.r0S10hCiHisQdoxYE5rfLAKP62Oj-r9gyy1K02bRsbQ"

# Batch Sizes: do not remove them from here.
#BATCHSIZE_LAYOUT=128
#BATCHSIZE_LINE=300
#BATCHSIZE_TEXTREC=550

RATE_LIMIT_HANDLER=500

# Queues configs — pipeline server (server_orchestrator) reads USE_TABLES / USE_CHARTS only from here
# when started via docker compose (see docker-compose service-pipeline environment).
USE_TABLES=True
USE_CHARTS=True

# ── Model loading flags ──────────────────────────────────────────────
# Set to 1 to start the container, 0 to skip it.
# USE_TABLES / USE_CHARTS (above) control pipeline routing in server_orchestrator, not these flags.
#   V1 (classic)    needs: layout + line + textrec + vision-model (+ chart-detection if charts enabled)
#   V2 (dots text)  needs: layout + vision-model
#   V3 (e2e dots)   needs: vision-model only (+ chart-detection if charts enabled)
#   V4 (hybrid)     needs: layout + line + textrec + vision-model (+ chart-detection if charts enabled)
ENABLE_LAYOUT_MODEL=1
ENABLE_LINE_MODEL=1
ENABLE_TEXTREC_MODEL=1
ENABLE_VISION_MODEL=1
ENABLE_CHART_MODEL=0

# Image Tag
VERSION_MODELS=10.1.0
VERSION_PIPELINE=10.2.4
