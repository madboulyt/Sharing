log:
  level: INFO

accessLog:
  filters:
    statusCodes:
      - "200-600"

http:
  hsts-header:
    headers:
      customResponseHeaders:
        Strict-Transport-Security: "max-age=604800"

providers:
  file:
    directory: /etc/traefik/configs
    watch: true

entryPoints:
  web:
    address: :80
    http:
      redirections:
        entryPoint:
          to: websecure
          scheme: https
  websecure:
    address: :443
    http:
      tls:
        options: cert-options@file
        certResolver: leresolver

certificatesResolvers:
  leresolver:
    acme:
      email: "platform_services@mozn.sa"
      storage: "/etc/traefik/acme/acme.json"
      caServer: "https://acme-v02.api.letsencrypt.org/directory"
      keyType: RSA2048
      httpChallenge:
        entryPoint: web
